Algoritmo "semnome"

Var

   contador : real
Inicio
      contador <- 0
      enquanto (contador <= 100) faca
               escreval(contador)
               contador <- contador + 1
      fimenquanto

Fimalgoritmo